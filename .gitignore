# Virtual environment
venv/
.venv/
env/

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
build/
dist/
*.egg-info/

# Environment variables (API keys)
.env
.env.local
!.env.example

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Coinbase credentials
cdp_api_key.json
cdp_api_key*.json
*.pem

# History files (user-specific data) -- allowlist: everything under
# history/ is ignored except the source/fixture files named below.
history/*
!history/__init__.py
!history/recorder.py
!history/test_expected_output.csv
!history/test_recommendation_data.json

# Temp files
*.tmp
# Accidental script(1) terminal captures (happened once; keep out of the repo)
typescript

# LunarCrush coin cache (generated by refresh_coin_cache.py)
coin_cache.json
coin_cache.backup.json

# Correlation data (collected metrics, user-specific)
correlation_data/

# DEX cache (token data, regenerated)
dex_cache/

# Paper trades (user-specific)
paper_trades/

# Live/DEX trade logs (per-user data; AGENTS.md rule #3). The 5 files that
# were previously tracked before this entry was added were untracked via
# `git rm --cached` in commit a2edfc2 (2026-07-19) and now stay local-only.
live_trades/
