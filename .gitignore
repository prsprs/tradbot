# Virtual environment
venv/
.venv/
env/

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
.pytest_cache/

# Environment variables (API keys)
.env
.env.local
!.env.example

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Coinbase credentials
cdp_api_key.json
cdp_api_key*.json
*.pem

# History files (user-specific data)
history/recommendations.json
history/recommendations.json.bak-*
history/executions.json
history/analyzer_state.json
history/*.csv
!history/test_expected_output.csv

# Temp files
*.tmp

# LunarCrush coin cache (generated by refresh_coin_cache.py)
coin_cache.json
coin_cache.backup.json

# Correlation data (collected metrics, user-specific)
correlation_data/

# DEX cache (token data, regenerated)
dex_cache/

# LP history (user-specific trading data)
history/lp/

# Paper trades (user-specific)
paper_trades/

# Live/DEX trade logs (per-user data; AGENTS.md rule #3 -- previously only
# enforced by convention, not actually covered here. The 5 files already
# tracked before this entry was added still need `git rm --cached`, see T11
# report.)
live_trades/
