# Dev/test-only dependencies. Install alongside requirements.txt to run the
# suite: see AGENTS.md's "Verify before and after" section for the exact
# command (./venv/bin/python -m pytest tests/ -q).

pytest>=8
