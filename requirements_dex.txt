# Requirements for DEX Trading Module (Solana via Jupiter)
#
# Install with: pip install -r requirements_dex.txt

# HTTP client for Jupiter API
httpx>=0.25.0

# Solana primitives for transaction signing
solders>=0.20.0
base58>=2.1.0

# Optional: Full Solana SDK (only if needed for advanced RPC calls)
# solana>=0.30.0
